{% extends 'base.html.twig' %}

{% block body %}
     <h1>Nueva Clase</h1>
    <div class="col-md-4"></div>
    <div class="col-md-4"> </div>
    <div class="col-md-4"></div>
   

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <a href="{{ path('clase_index') }}?id={{ cursada }}">Regresar</a>

{% endblock body %}
