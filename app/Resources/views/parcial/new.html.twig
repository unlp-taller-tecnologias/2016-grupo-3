{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parcial creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <a href="{{ path('parcial_index') }}?id={{ cursada }}">Regresar</a>
{% endblock %}
