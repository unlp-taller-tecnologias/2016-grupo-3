{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parcial</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parcial.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ parcial.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ parcial.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parcial_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('parcial_edit', { 'idParcial': parcial.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
